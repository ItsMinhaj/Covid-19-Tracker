import 'package:flutter/material.dart';

class WorldsCurrentState extends StatelessWidget {
  const WorldsCurrentState({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
